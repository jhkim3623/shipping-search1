# ==========================================================
#  출고 이력 검색  -  requirements.txt  (최종 확정본)
#  확정일: 2026-09-21
#
#  기준: 실제 배포 환경에서 확인된 버전
#        (앱 사이드바 "시스템 정보" 실측값)
#        Python 3.14.7 / Streamlit 1.64.0
#        pandas 3.0.6 / numpy 2.5.3 / plotly 7.1.0
#
#  원칙: "지금 정상 동작하는 버전을 그대로 고정한다"
#  주의: 아래 == 로 고정한 버전은 현재 앱이 정상 동작 중인
#        실측 버전입니다. 임의로 낮추지 마세요.
# ==========================================================

# ---------- Streamlit ----------
# 현재 설치/동작 버전과 동일. PyPI 최신 안정판이기도 합니다.
streamlit==1.64.0

# ---------- 데이터 처리 ----------
# [중요] pandas 는 현재 3.0.6 에서 정상 동작 중이므로 3.x 를 그대로 고정합니다.
#        (2.x 로 낮추면 오히려 동작이 달라질 수 있습니다)
pandas==3.0.6

# numpy 도 현재 2.5.3 에서 정상 동작 중이므로 그대로 고정합니다.
numpy==2.5.3

openpyxl>=3.1.0,<4.0.0
pyarrow>=16.1.0

# ---------- 시각화 / 부가 라이브러리 ----------
# [중요] plotly 는 현재 7.1.0 에서 정상 동작 중이므로 7.x 를 그대로 고정합니다.
#        (5.x 로 낮추면 차트 동작이 달라질 수 있습니다)
plotly==7.1.0

xlsxwriter>=3.2.0,<4.0.0
holidays>=0.48
